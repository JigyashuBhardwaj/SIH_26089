# utils/

Small, generic helper functions specific to the mobile app (formatting,
date helpers for display, etc.) that aren't domain rules. Domain logic
(booking validation, matching) belongs in `../shared`, not here.

Empty for now.
