# constants/

App-wide constant values that are specific to the mobile app's UI (e.g.
color palette, spacing scale, route names) — as opposed to domain
constants like booking rule thresholds, which live in `../shared/booking`
since the backend needs them too.

Empty for now.
